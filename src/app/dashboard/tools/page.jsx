import ToolsDirectory from "@/modules/tools/tools.directory";

export default function DashboardToolsPage() {
  return (
    <ToolsDirectory
      heading="Tools workspace"
      subtitle="Search and open tools directly inside your workspace."
    />
  );
}
